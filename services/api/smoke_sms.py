# The two-way SMS/Twilio feature this smoke test covered was removed at the
# product owner's request in favor of an email-based approve/deny flow --
# see smoke_email_approval.py. Left as an inert placeholder only because
# this sandbox can't delete files on the mounted project folder -- safe to
# delete for real via `git rm services/api/smoke_sms.py`.
